//Minimum
const min = (x, y) => {
    if (x <= y)                                 //simple condition check
        return x;
    else
        return y;
};
console.log(min(1,'a'));                  //all strings are before numbers I guess